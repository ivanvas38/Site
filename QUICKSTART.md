# Быстрый старт

Следуйте этим шагам чтобы быстро запустить проект.

## Требования

- Node.js >= 16.0.0
- npm >= 8.0.0
- MySQL >= 8.0

## Шаг 1: Клонирование (если необходимо)

```bash
git clone <repository>
cd messenger
```

## Шаг 2: Backend Setup

```bash
cd backend

# Установка зависимостей
npm install

# Копирование примера конфигурации
cp .env.example .env

# Редактируйте .env файл с вашими данными
# Важно: установите корректные credentials для MySQL
nano .env
```

### Содержимое .env (минимум)

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=messenger
JWT_SECRET=change_this_to_random_string
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000
```

## Шаг 3: Frontend Setup

```bash
cd ../frontend

# Установка зависимостей
npm install
```

## Шаг 4: Запуск приложения

Откройте **два** отдельных терминала.

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

Должны увидеть: `Server is running on port 5000`

**Терминал 2 - Frontend:**
```bash
cd frontend
npm start
```

Должны увидеть: Браузер откроется на `http://localhost:3000`

## Проверка

1. Backend должен быть доступен: http://localhost:5000/health
2. Frontend должен загрузиться: http://localhost:3000
3. Консоль браузера не должна содержать ошибок

## Создание БД (опционально)

Если хотите создать таблицы в БД:

```sql
-- Подключитесь к MySQL
mysql -u root -p

-- Создайте БД
CREATE DATABASE messenger;
USE messenger;

-- Создайте таблицы (см. INSTALLATION.md для полного скрипта)
```

## Окончание

Теперь вы готовы к разработке! 

Дальше:
- Читайте [docs/API.md](./docs/API.md) для API документации
- Читайте [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md) для понимания структуры
- Смотрите [DEVELOPMENT.md](./docs/DEVELOPMENT.md) для правил разработки

## Общие проблемы

| Проблема | Решение |
|---------|---------|
| Port 5000 занят | Измените PORT в `.env` |
| MySQL connection error | Проверьте credentials в `.env` |
| npm install не работает | `npm cache clean --force && rm -rf node_modules && npm install` |
| CORS error | Проверьте CORS_ORIGIN в `.env` backend |

## Помощь

Если что-то не работает:

1. Проверьте что Node.js установлен: `node -v`
2. Проверьте что MySQL запущен
3. Проверьте консоль браузера на ошибки
4. Прочитайте документацию в папке `docs/`

Успехов в разработке! 🚀
