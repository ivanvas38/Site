# Мессенджер

Современный веб-мессенджер с поддержкой групповых чатов, файловой передачи и уведомлений.

## Быстрая установка

Для автоматической установки всего необходимого на Ubuntu 22.04 выполните:

```bash
# Клонируйте репозиторий
git clone <URL_РЕПОЗИТОРИЯ> messenger
cd messenger

# Сделайте скрипт исполняемым
chmod +x scripts/setup.sh

# Запустите автоматическую установку
./scripts/setup.sh
```

Это всё! Скрипт автоматически:

✅ Проверит права доступа и систему  
✅ Установит Node.js 18+ и npm  
✅ Установит MySQL 8.0  
✅ Установит PM2 для управления процессами  
✅ Установит и настроит Nginx  
✅ Создаст базу данных с таблицами  
✅ Настроит .env файлы  
✅ Установит SSL сертификат (для домена)  
✅ Запустит все сервисы  
✅ Проверит работоспособность  

## Расширенные параметры

### Установка с указанием домена
```bash
./scripts/setup.sh --domain your-domain.com
```

### Установка с указанием репозитория
```bash
./scripts/setup.sh --repo https://github.com/username/messenger.git
```

### Установка без SSL
```bash
./scripts/setup.sh --domain localhost --no-ssl
```

### Помощь
```bash
./scripts/setup.sh --help
```

## Что устанавливается

- **Node.js 18+** - JavaScript runtime
- **npm** - менеджер пакетов
- **MySQL 8.0** - база данных
- **PM2** - менеджер процессов
- **Nginx** - веб-сервер
- **Let's Encrypt** - SSL сертификаты

## Структура проекта

```
messenger/
├── backend/              # Node.js сервер
│   ├── src/             # Исходный код
│   ├── package.json     # Зависимости
│   └── .env            # Настройки
├── frontend/            # React/Vue приложение
│   ├── src/            # Исходный код
│   ├── dist/           # Собранные файлы
│   └── .env            # Настройки
├── scripts/             # Скрипты установки
│   ├── setup.sh        # Основной скрипт
│   └── database-setup.sql  # База данных
├── logs/               # Логи установки
└── README.md          # Этот файл
```

## Управление после установки

### Мониторинг сервисов
```bash
pm2 status              # Статус процессов
pm2 logs               # Логи приложения
pm2 restart all        # Перезапустить все
pm2 stop all           # Остановить все
```

### Управление MySQL
```bash
sudo systemctl status mysql     # Статус MySQL
sudo systemctl restart mysql    # Перезапуск MySQL
```

### Управление Nginx
```bash
sudo systemctl status nginx     # Статус Nginx
sudo nginx -t                   # Проверка конфигурации
sudo systemctl reload nginx     # Перезагрузка конфигурации
```

## Доступ к приложению

После установки приложение будет доступно по адресу:

- **Локально**: http://localhost
- **С доменом**: https://your-domain.com

## Безопасность

- SSL сертификат Let's Encrypt (автоматическое обновление)
- Защищенная конфигурация MySQL
- JWT токены для аутентификации
- Хеширование паролей
- CORS настройки

## Поддержка

- Логи установки сохраняются в папке `logs/`
- Для диагностики используйте: `pm2 logs messenger-backend`
- Проверка портов: `sudo netstat -tlnp`

## Системные требования

- Ubuntu 22.04 (рекомендуется)
- 2GB RAM (минимум)
- 10GB свободного места
- Права sudo
- Стабильное интернет-соединение

---

**🎉 Готово!** Ваш мессенджер установлен и готов к использованию!