# TeleApp - React Frontend + Express Backend Integration

Современное полнофункциональное приложение с React фронтенд-частью в стиле Telegram и Express бэкенд-частью с MySQL базой данных. Интегрированная система аутентификации с JWT, сохранением сессии и защитой маршрутов.

## Возможности

### Фронтенд
- 🎨 **Красивый дизайн** - Минималистичный интерфейс в стиле Telegram
- 📱 **Responsive** - Адаптивный дизайн для всех устройств
- 🌙 **Темная тема** - Встроенная поддержка темной темы
- 🔐 **Аутентификация** - Полная система входа и регистрации
- ✨ **Анимации** - Плавные переходы и анимации
- 🇷🇺 **Русский язык** - Полная поддержка русского языка
- 📦 **TypeScript** - Полная типизация для надежности
- 🎯 **Lucide Icons** - Красивые иконки

### Бэкенд
- 🚀 **Express.js** - Мощный REST API фреймворк
- 🗄️ **MySQL** - Надежная база данных
- 🔐 **JWT Authentication** - Безопасная аутентификация
- 🔒 **bcrypt** - Безопасное хеширование паролей
- 🛡️ **CORS** - Защита от кросс-доменных запросов
- ✅ **Error Handling** - Полная обработка ошибок

## Технологический стек

### Фронтенд
- **React 18** - UI библиотека
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Утилит-первый CSS фреймворк
- **Vite** - Современный build инструмент
- **Lucide React** - Иконки

### Бэкенд
- **Node.js** - Runtime environment
- **Express.js** - Web фреймворк
- **MySQL** - База данных
- **bcrypt** - Хеширование паролей
- **jsonwebtoken** - JWT токены
- **dotenv** - Управление переменными окружения

## Структура проекта

```
.
├── frontend/
│   ├── src/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   ├── index.css
│   │   ├── pages/
│   │   │   ├── LandingPage.tsx
│   │   │   ├── LoginPage.tsx
│   │   │   ├── RegisterPage.tsx
│   │   │   └── ProtectedPage.tsx
│   │   ├── context/
│   │   │   ├── AuthContext.tsx
│   │   │   └── NavigationContext.tsx
│   │   ├── components/
│   │   │   └── PrivateRoute.tsx
│   │   └── utils/
│   │       └── api.ts
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
│
├── backend/
│   ├── src/
│   │   ├── app.js
│   │   ├── config/
│   │   │   └── database.js
│   │   ├── models/
│   │   │   └── User.js
│   │   ├── controllers/
│   │   │   └── authController.js
│   │   ├── routes/
│   │   │   └── auth.js
│   │   └── middleware/
│   │       └── auth.js
│   ├── package.json
│   └── .env.example
│
└── README.md
```

## Установка и запуск

### Требования
- Node.js 16 или выше
- npm или yarn
- MySQL 5.7 или выше

### Установка зависимостей фронтенд-части

```bash
npm install
```

### Разработка фронтенд-части

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

### Проверка типов

```bash
npm run type-check
```

### Линтинг

```bash
npm run lint
```

### Установка и запуск бэкенд-части

```bash
cd backend
npm install
npm start
```

Бэкенд будет доступен по адресу `http://localhost:3000`

## Конфигурация

### Фронтенд
Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:3000/api
```

### Бэкенд
Создайте файл `.env` в директории `backend/`:

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=teleapp_db
JWT_SECRET=your_jwt_secret_key
PORT=3000
```

## API Endpoints

### Authentication
- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход с email/username и паролем
- `POST /api/auth/logout` - Выход из аккаунта
- `GET /api/auth/me` - Получить информацию о текущем пользователе
- `POST /api/auth/refresh` - Обновить JWT токен

## Страницы

### Landing Page
Стартовая страница с информацией о приложении и кнопками "Регистрация" и "Авторизация".

**Функциональность:**
- Привлекательный дизайн с градиентами
- Описание возможностей
- Быстрый переход к регистрации/входу

### Login Page
Форма входа в приложение.

**Функциональность:**
- Ввод email и пароля
- Опция "Запомнить меня"
- Возможность восстановления пароля
- Loading состояние при отправке
- Обработка ошибок
- Редирект на защищенную страницу при успешном входе

**Демо данные:**
- Email: demo@example.com
- Пароль: Demo123456

### Register Page
Форма регистрации новой учетной записи.

**Функциональность:**
- Ввод email, имени пользователя, пароля
- Подтверждение пароля
- Валидация на клиенте
- Русские сообщения об ошибках
- Loading состояние при отправке
- Обработка ошибок (занятый email/username)
- Автоматический вход после регистрации

### Protected Page
Защищенная страница, доступная только авторизованным пользователям.

**Функциональность:**
- Отображение информации текущего пользователя
- Кнопка выхода из аккаунта
- Сохранение сессии при перезагрузке страницы

## Функциональность

### Аутентификация
- Регистрация с валидацией на клиенте и сервере
- Вход с email/username
- Сохранение JWT токена в localStorage
- Автоматическое восстановление сессии при загрузке приложения
- Выход из аккаунта с очисткой токена

### Защита маршрутов
- Компонент PrivateRoute для защиты защищённых страниц
- Автоматический редирект неавторизованных пользователей на страницу входа
- Сохранение текущего маршрута для редиректа после входа

### Обработка ошибок
- Специфичные сообщения об ошибках при регистрации
- Обработка ошибок неверного пароля
- Обработка ошибок занятого email/username
- Очистка ошибок при очистке форм

### Loading состояния
- Отключение кнопок во время отправки форм
- Отображение loader-а при обработке запроса
- Предотвращение двойной отправки

### Сохранение сессии
- JWT токен сохраняется в localStorage
- При загрузке приложения проверяется наличие токена
- Токен автоматически используется для всех API запросов
- Сессия сохраняется при перезагрузке страницы

## Валидация

### Email
- Обязательное поле
- Проверка формата email

### Пароль
- Минимум 8 символов
- Должен содержать прописные буквы
- Должен содержать строчные буквы
- Должен содержать цифры

### Имя пользователя
- Минимум 3 символа
- Только буквы, цифры и подчеркивание

## Context API

### AuthContext
Управляет состоянием аутентификации приложения.

**Методы:**
- `login()` - Вход пользователя
- `register()` - Регистрация нового пользователя
- `logout()` - Выход из аккаунта
- `clearError()` - Очистка ошибок
- `restoreSession()` - Восстановление сессии из localStorage

**Состояние:**
- `user` - Текущий пользователь
- `isAuthenticated` - Статус аутентификации
- `isLoading` - Статус загрузки
- `error` - Текст ошибки

### NavigationContext
Управляет навигацией между страницами.

**Методы:**
- `navigate()` - Навигация к странице

**Состояние:**
- `currentPage` - Текущая страница

## API Integration

Модуль `api.ts` предоставляет функции для работы с API:

```typescript
import { authApi } from '@/utils/api'

// Вход
await authApi.login({ email, password, rememberMe })

// Регистрация
await authApi.register({ email, username, password })

// Выход
await authApi.logout()

// Получить текущего пользователя
await authApi.getCurrentUser()
```

## Стилизация

### Tailwind CSS
Проект использует Tailwind CSS для стилизации. Основные цвета и конфигурация находятся в `tailwind.config.js`.

**Основные цвета:**
- Primary: `#0088CC` (Telegram Blue)
- Text: `#000000` / `#FFFFFF` (dark mode)
- Border: `#DADCE0`

### Анимации
- `fadeIn` - Плавное появление
- `slideUp` - Скользящее появление снизу
- `pulse` - Пульсирующая анимация

## Структура компонентов

Все компоненты используют функциональный подход с React Hooks.

## Тестирование

### Чек-лист регистрации и входа
- [ ] Регистрация нового пользователя
- [ ] Вход с корректными данными
- [ ] Обработка неверного пароля
- [ ] Обработка занятого email/username
- [ ] Обновление страницы - сохранение сессии (если токен в localStorage)
- [ ] Выход из аккаунта
- [ ] Защита маршрутов - попытка доступа к защищенной странице без авторизации

## Лицензия

MIT

## Автор

Created as part of TeleApp project.
